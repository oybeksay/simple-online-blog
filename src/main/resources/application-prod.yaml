spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/your_database_name?currentSchema=test'
    username: postgres
    password: your_password
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

